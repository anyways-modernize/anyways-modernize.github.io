# Build artifacts generated by .github/workflows/build-posts.yml
# These are produced in CI and deployed straight from the Pages artifact,
# so they should not be tracked in the repo.
/posts/*.html
/scrolls.html
/data/search.json
/sitemap.xml
/og-image.png
/og/
